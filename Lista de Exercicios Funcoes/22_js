function anuidadeAssociacao(mes, anuidade) {
    const juros = 0.05
    if (mes > 0 && mes < 13) {
        atraso = mes - 1
        return (anuidade * ((1 + juros)** atraso)).toFixed(2)
    }else {
        return 'Mês inválido'
    }
}
console.log(anuidadeAssociacao(4, 100))