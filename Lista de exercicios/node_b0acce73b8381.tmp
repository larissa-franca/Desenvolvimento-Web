function repeticao(min, max) {
    const valor = Math.random() * (min, max) + min
    return Math.floor(valor)}

    {let opcao = 0
    while (opcao != -1) {
        opcao = repeticao(0, 12) 
    }
}
console.log('Hello World!')